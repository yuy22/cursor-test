# AGENTS.md

## Cursor Cloud specific instructions

This is an empty starter repository (`cursor-test`) containing only a `README.md`.

- **No application code, dependencies, build system, or services** exist yet.
- **No lint, test, or build commands** are configured.
- Standard dev tooling available in the environment: Node.js, npm, Python 3, git.
- When application code is added, update this file with service descriptions, run/test/lint/build commands, and any non-obvious caveats.
