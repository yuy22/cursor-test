# -*- coding: utf-8 -*-
"""
【使用方法】
1. 先运行字号分析：python clean_docx_v3.py --analyze
2. 根据分析结果填写下面的配置
3. 正式清洗：python clean_docx_v3.py
4. 图片审查：python clean_docx_v3.py --review-images

【配置示例 1】含章节分节的教育类书籍（如《低头找幸福》）
SIZE_TO_HEADING = {
    23.5: 2,   # ## 大章标题（"教育是一门艺术"）
    16.0: 3,   # ### 时期节标题（"1998—2002 忽如一夜春风来"）
    14.5: 4,   # #### 子节（"◎ 分数准备课教学设计"）
    13.5: 4,   # #### 固定小标签（"访谈手记"）
}
KNOWN_HEADINGS_H4 = {'访谈手记', '苏霍姆林斯基'}

【配置示例 2】问题式结构书籍（如《新课标数学课例式解读》）
SIZE_TO_HEADING = {
    16.0: 2,   # ## 上编/下编 分部
    15.5: 2,   # ## 主要章节
    14.5: 2,   # ## 序言章节
    14.0: 3,   # ### 子节
    13.5: 3,   # ### 课例标题
    12.5: 4,   # #### 第X章/问题X
    12.0: 4,   # #### 小字课例
}
KNOWN_HEADINGS_H4 = {'课例'}

【配置示例 3】纯课例讲解类书籍（无章节分节）
SIZE_TO_HEADING = {
    18.0: 2,   # ## 大章
    14.0: 3,   # ### 课例标题
    12.0: 4,   # #### 教学环节
}
KNOWN_HEADINGS_H4 = set()
"""

# ──────────────────────────────────────────────
# 新书配置：复制以下内容到 clean_docx_v3.py 顶部
# ──────────────────────────────────────────────
from pathlib import Path
import re

INPUT        = Path("你的文件.docx")
OUT_DIR      = Path("output/书名_cleaned")
BOOK_TITLE   = "书名"
BOOK_AUTHOR  = "作者名"

# 第一步：先运行 --analyze 获取字号分布，再填写下面的映射
SIZE_TO_HEADING = {
    # 23.5: 2,   # 大字号加粗 → ## 章标题
    # 16.0: 3,   # 中字号 → ### 节标题
    # 13.5: 4,   # 小字号 → #### 子标题
}

# 防 OCR 字号漂移：这些文本固定为 #### 标题
KNOWN_HEADINGS_H4 = {
    # '访谈手记',
    # '课例背景',
}

# 本书特有页眉正则（命中即删整行）
BOOK_HEADER_PATTERNS = [
    # re.compile(r'^书名\s*$'),
    # re.compile(r'^\d+\s+作者名\s*[：:]\s*书名'),
]
